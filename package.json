{
    "name": "beat-webgl",
    "version": "1.0.0",
    "description": "A game engine made using Typescript",
    "main": "public/dist/main.js",
    "types": "public/dist/**/**.d.ts",
    "scripts": {
        "prepare": "yarn clean && npm run build",
        "prepublishOnly": "eslint --ext ts,js .",
        "clean": "rm -rf dist && rm -rf public/dist",
        "http": "http-server --cors public",
        "watch": "yarn clean && cd public && tsup --format iife --dts --keep-names --sourcemap inline ../app.ts ../core/**/*.ts --watch ../core",
        "build": "yarn clean && cd public && tsup --format iife --dts --keep-names --sourcemap inline ../app.ts ../core/**/*.ts"
    },
    "keywords": [],
    "author": "Beatzoid",
    "license": "MIT",
    "devDependencies": {
        "@typescript-eslint/eslint-plugin": "^4.33.0",
        "@typescript-eslint/parser": "^4.33.0",
        "eslint": "^7.32.0",
        "eslint-plugin-jsdoc": "^36.1.1",
        "http-server": "^13.0.2",
        "tsup": "^5.9.0",
        "typescript": "^4.5.2"
    }
}
